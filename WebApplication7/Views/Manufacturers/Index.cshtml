@model IEnumerable<WebApplication7.Models.Manufacturer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameMan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameMan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
